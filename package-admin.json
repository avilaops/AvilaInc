{
  "name": "avila-inc-admin",
  "version": "1.0.0",
  "description": "Painel administrativo da Avila Inc - GitHub Pages",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/views/* dist/ && cp -r src/public/* dist/ 2>/dev/null || true",
    "dev": "node server.js",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avilaops/manager.git"
  },
  "keywords": ["admin", "dashboard", "avila-inc"],
  "author": "Avila Inc",
  "license": "MIT"
}